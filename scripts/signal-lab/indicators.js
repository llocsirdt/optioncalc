'use strict';
// Moved to server/src/candle-spread/signals (server owns the canonical analysis math — one source of truth for backtest+live parity).
module.exports = require('../../server/src/candle-spread/signals/indicators');
